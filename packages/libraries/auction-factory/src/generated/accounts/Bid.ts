/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link Bid}
 * @category Accounts
 * @category generated
 */
export type BidArgs = {
  bump: number
  auction: web3.PublicKey
  bidIdx: beet.bignum
  bidder: web3.PublicKey
  submittedAt: beet.bignum
  amount: beet.bignum
}

export const bidDiscriminator = [143, 246, 48, 245, 42, 145, 180, 88]
/**
 * Holds the data for the {@link Bid} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class Bid implements BidArgs {
  private constructor(
    readonly bump: number,
    readonly auction: web3.PublicKey,
    readonly bidIdx: beet.bignum,
    readonly bidder: web3.PublicKey,
    readonly submittedAt: beet.bignum,
    readonly amount: beet.bignum
  ) {}

  /**
   * Creates a {@link Bid} instance from the provided args.
   */
  static fromArgs(args: BidArgs) {
    return new Bid(
      args.bump,
      args.auction,
      args.bidIdx,
      args.bidder,
      args.submittedAt,
      args.amount
    )
  }

  /**
   * Deserializes the {@link Bid} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [Bid, number] {
    return Bid.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Bid} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey
  ): Promise<Bid> {
    const accountInfo = await connection.getAccountInfo(address)
    if (accountInfo == null) {
      throw new Error(`Unable to find Bid account at ${address}`)
    }
    return Bid.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      '2jbfTkQ4DgbSZtb8KTq61v2ox8s1GCuGebKa1EPq3tbY'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, bidBeet)
  }

  /**
   * Deserializes the {@link Bid} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [Bid, number] {
    return bidBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link Bid} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return bidBeet.serialize({
      accountDiscriminator: bidDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Bid}
   */
  static get byteSize() {
    return bidBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Bid} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      Bid.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link Bid} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === Bid.byteSize
  }

  /**
   * Returns a readable version of {@link Bid} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      bump: this.bump,
      auction: this.auction.toBase58(),
      bidIdx: (() => {
        const x = <{ toNumber: () => number }>this.bidIdx
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      bidder: this.bidder.toBase58(),
      submittedAt: (() => {
        const x = <{ toNumber: () => number }>this.submittedAt
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      amount: (() => {
        const x = <{ toNumber: () => number }>this.amount
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const bidBeet = new beet.BeetStruct<
  Bid,
  BidArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['bump', beet.u8],
    ['auction', beetSolana.publicKey],
    ['bidIdx', beet.u64],
    ['bidder', beetSolana.publicKey],
    ['submittedAt', beet.u64],
    ['amount', beet.u64],
  ],
  Bid.fromArgs,
  'Bid'
)
